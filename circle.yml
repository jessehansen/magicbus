machine:
  timezone:
    America/Denver
  node:
    version: 4
  services:
    - rabbitmq-server
dependencies:
  pre:
    - rm -rf node_modules/
  post:
    - rm -r ~/.gradle
deployment:
  release:
    tag: /v.*/
    owner: LeisureLink
    commands:
      - npm publish
